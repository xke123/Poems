name: poems
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+3
  path_provider: ^2.0.1
  path: ^1.8.0
  sqflite_common_ffi: ^2.1.0
  provider: ^6.0.0
  cupertino_icons: ^1.0.8
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"  # 图标路径

flutter:
  uses-material-design: true

  assets:
    - assets/database/poems.db
    - assets/images/
    - assets/database/collections.json
    - assets/image/
    - assets/dynasty/
    - assets/dynasty/dynasty-icon/
    - assets/image/dynasty-icon/